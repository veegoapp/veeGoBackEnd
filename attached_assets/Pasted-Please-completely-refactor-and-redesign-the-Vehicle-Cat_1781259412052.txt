Please completely refactor and redesign the Vehicle Catalog management workspace under `artifacts/admin-dashboard/src/components/VehicleCatalogTab.tsx` (and related route files). The current flat list layout is highly unorganized and inefficient. 

Implement a clean, hierarchical, nested drill-down structure and integrate a dynamic dynamic pricing category rule engine.

Follow these explicit architectural and implementation specifications:

---

### 1. HIERARCHICAL DRILL-DOWN UX/UI REDESIGN

#### Step A: Master Level — Approved Brands View
- Remove the side-by-side or stacked flat lists of Brands and Models.
- The default screen must only display the **Approved Brands** grid/table (e.g., Toyota, Hyundai, Kia, Chevrolet) along with the existing **Approved Colors** catalog as an independent section on the side or bottom.
- Each Brand row/card must be clickable, acting as a link or triggering a view-state that navigates the admin deeper.

#### Step B: Second Level — Brand-Specific Models View
- Clicking a specific Brand (e.g., "Toyota") must transition the UI (via nested routing or clean sub-state management) to a dedicated **Brand Profile View** titled: `[Brand Name] Approved Models`.
- This view must query and list **only** the models explicitly belonging to that specific brand (e.g., Corolla, Yaris, Fortuner).
- Include an "Add New Model" button here that automatically binds the new model to this parent Brand ID.

#### Step C: Third Level — Model-Specific Manufacture Years View
- Clicking a specific Model (e.g., "Corolla") must drill down into a third-level view showing all registered **Years of Manufacture** for that specific model (e.g., 2020, 2022, 2025).
- Include an "Add Year" control to register a new manufacturing year under this model.

---

### 2. DYNAMIC PRICING CATEGORY RULE ENGINE (THE RULES)

- On the **Years of Manufacture** level (Step C), each individual year row must feature an inline configuration control and an **"Edit Category Rule"** action.
- Admins must be able to assign or update the specific operational tier/category for that specific model-year combination.
- The input must be a controlled Dropdown/Select menu with exactly these three tiered categories:
  1. `Economy` (اقتصادي)
  2. `Economy Plus` (اقتصادي بلس)
  3. `Comfort` (كومفورت)
- **API Mapping:** When the admin updates the selection, fire a `PATCH` request to the backend model-year lookup endpoint (e.g., `/api/admin/vehicle-catalog/models/:modelId/years/:yearId` or the designated catalog rule update API) payload: `{ pricingCategory: "Economy" | "EconomyPlus" | "Comfort" }`.
- Ensure real-time query cache invalidation and a success toast confirmation once the server accepts the category change.

---

### 3. NAVIGATION & BREADCRUMBS
- To prevent the admin from getting lost in the deep nesting, implement an interactive **Breadcrumb Trail** navigation header at the top of the workspace (e.g., `Vehicle Catalog > Toyota > Corolla > 2022`).
- Clicking any parent segment in the breadcrumb trail must safely transition the UI state back to that respective level.

---

### TECHNICAL REQUIREMENTS:
- Maintain full compatibility with existing backend CRUD schemas for brands and models, only altering the presentation logic, state drilling, and endpoint parameter parsing.
- Use smooth loading skeletons for transitional states when drilling down between levels.
- Strictly adhere to the pre-existing design system tokens, layout constraints, and Tailwind classes.