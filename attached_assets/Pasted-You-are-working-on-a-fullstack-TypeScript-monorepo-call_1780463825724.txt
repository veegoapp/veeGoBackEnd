You are working on a fullstack TypeScript monorepo called VeeGo (a ride-hailing/transport admin dashboard).
The project has two artifacts:
- artifacts/admin-dashboard (React + Vite + TailwindCSS + shadcn/ui)
- artifacts/api-server (Express 5 + TypeScript + Socket.io)

The API server is the single source of truth for ALL data and business logic.
Do NOT use localStorage, hardcoded data, or client-side state as a substitute for server data.
Do NOT break any existing functionality.
Fix ONLY the issues listed below, one by one, in order.
After finishing ALL fixes, provide a summary of exactly what was changed and in which files.

---

ISSUE 1 — Wallet debit not working correctly
File: artifacts/admin-dashboard/src/pages/users.tsx
Problem: When the admin selects "debit" for wallet adjustment, the amount is always sent as a positive number. The server expects a negative amount for debit operations.
Fix: Before sending the request, if walletType === "debit", multiply the amount by -1 before sending it to /admin/wallet/refund.

---

ISSUE 2 — Drivers page fetches 300 records with no real pagination
File: artifacts/admin-dashboard/src/pages/drivers.tsx
Problem: The page fetches limit: 300 in one shot with no server-side pagination.
Fix: Change to limit: 15 per page, add a page state variable, and add proper pagination controls (previous/next) using the existing Pagination component already used in other pages. Use the total count returned by the API to calculate whether next page exists.

---

ISSUE 3 — Payments and Wallet pages overlap (both show transactions)
Files: artifacts/admin-dashboard/src/pages/payments.tsx and artifacts/admin-dashboard/src/pages/wallet.tsx
Problem: Both pages display wallet transactions, causing confusion and duplication.
Fix:
- wallet.tsx: keep ONLY the transactions table with filters (type, date range, userId search) and the refund form. Remove any driver analytics or payout sections if they exist in this file.
- payments.tsx: keep ONLY driver analytics, top earners, and driver payouts table with payout confirmation. Remove the general transactions table if it exists in this file.
This way Wallet = user transactions, Payments = driver earnings and payouts.

---

ISSUE 4 — Chat inbox opens a duplicate Socket.io connection
File: artifacts/admin-dashboard/src/pages/chat-inbox.tsx
Problem: The page creates its own Socket.io connection using io() directly, while the app already has a global admin socket managed by artifacts/admin-dashboard/src/hooks/useAdminSocket.ts.
Fix: Remove the local io() socket instance from chat-inbox.tsx. Instead, use the existing useAdminSocket hook for real-time events. For sending messages and fetching conversations, keep using the REST API calls (adminFetch). Only the real-time listening part should use the shared socket.

---

ISSUE 5 — Sidebar nav items marked "coming soon" but pages are fully working
File: artifacts/admin-dashboard/src/components/layout/app-layout.tsx
Problem: Several nav sub-items have comingSoon: true but their corresponding pages and API endpoints are fully implemented.
Fix: Remove comingSoon: true from the following items:
- Under Pricing: the "delivery" sub-item (/pricing/delivery)
- Under Reports: ALL sub-items (revenue, trips, drivers, passengers, zones, services, promo codes, complaints) — these are all rendered inside the existing reports.tsx page as tabs, so they should either link to /reports or be removed as separate sub-items entirely. Recommended: remove all reports sub-items from the sidebar since the reports page handles everything internally with its own tabs.

---

ISSUE 6 — Vehicles page shows driver ID instead of driver name
File: artifacts/admin-dashboard/src/pages/vehicles.tsx
Problem: The vehicles table displays the raw driverId number instead of the driver's name.
Fix: The API already returns driver data alongside vehicles (or can be fetched). Display the driver name in the table. If the API response does not include driver name, add a join or lookup on the server side in artifacts/api-server/src/routes/vehicles.ts so that each vehicle row includes driverName. Then display driverName in the table, falling back to "—" if null.

---

ISSUE 7 — Reports page export button is not connected
File: artifacts/admin-dashboard/src/pages/reports.tsx
Problem: There is a Download icon button visible in the UI but it has no onClick handler and does nothing.
Fix: Connect the export button to actually export the currently visible report data as a CSV file. Use the existing exportCSV utility already present in artifacts/admin-dashboard/src/lib/export.ts. Each report tab should export its own relevant data (e.g. revenue tab exports the time series data, drivers tab exports the driver list, etc).

---

ISSUE 8 — Live tracking map has no driver filter
File: artifacts/admin-dashboard/src/pages/live-tracking.tsx
Problem: All drivers are shown on the map with no way to filter by status or search by name.
Fix: Add a search input and a status filter dropdown (all / online / busy / offline / suspended) above the driver list panel. Filter both the list AND the map markers based on the selected filters. The filtering should be client-side only since the data is already loaded.

---

IMPORTANT RULES:
- The API server handles ALL business logic. Do not move logic to the client.
- Do not change the database schema or any existing API endpoint signatures.
- Do not remove any existing features.
- Do not change any UI design or styling beyond what is strictly necessary for the fix.
- After completing all fixes, reply with a structured summary: list each issue number, the files changed, and one sentence describing what was done.