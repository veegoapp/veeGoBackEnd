I need a full, deep and accurate backend diagnosis of the entire project. Do NOT assume anything. Only rely on actual code and runtime behavior.

I want you to investigate and report the real root causes of all current issues step by step.

1) Server startup analysis
How exactly is the API server started (run command / setup script / workflows)?
Is the API server running as a persistent process or stopping after setup?
Is there any conflict between the API server and admin dashboard processes or ports?
2) Routing analysis
Verify all backend routes and confirm if everything is correctly under /api
Compare routes used by:
admin dashboard
passenger app
driver app
Provide a full list of actual registered API routes in the backend
3) Database verification
Confirm the backend is connected to the correct DATABASE_URL from secrets
Check if there is ANY fallback, mock data, seed override, or in-memory data
Confirm that all data (users, trips, bookings, customers) is truly being read from the same database
4) Admin dashboard missing data issue
Why does the admin dashboard not show:
users
trips
bookings
Identify the exact cause (API issue, auth issue, wrong base URL, wrong endpoint, or DB issue)
5) 502 error in admin dashboard
Explain the real root cause of the 502 error
Is it caused by port conflict, server crash, proxy misconfiguration, or something else?
Do NOT guess — only confirm from logs/code
6) Passenger app login issue
Why does login fail with "invalid credentials" while signup works?
Check password hashing, user creation, DB consistency, and auth flow
Verify token generation and validation logic
7) Environment variables audit
List all environment variables used at runtime
Confirm if any are overridden by .env, code defaults, or secrets mismatch
8) Final report

Provide a clear final summary including:

Root cause of each issue
Exact file or line responsible
Precise fix steps for each problem

IMPORTANT:

Do NOT assume anything
Do NOT give theories
Only use real code analysis and runtime logs
If something is missing, explicitly say: "Not found in the project"