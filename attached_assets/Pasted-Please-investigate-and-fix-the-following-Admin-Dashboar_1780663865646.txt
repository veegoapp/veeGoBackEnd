Please investigate and fix the following Admin Dashboard issues. Do not make assumptions. Trace the frontend, backend endpoints, permissions, API calls, and database operations until the real root cause is found and fixed.

1. Drivers Page Not Showing Data

* In the sidebar there is a Drivers page and a Pending Verification section beneath it.
* The Drivers page already exists and was previously working.
* Currently when I click Drivers, no driver records are displayed.
* Verify that:

  * The page is correctly connected to the sidebar route.
  * The page component is rendering.
  * The API call is being made.
  * The API endpoint returns driver data.
  * The frontend is reading the response correctly.
  * No permissions, filtering, pagination, or state issues are hiding the data.
* Fix the issue so all registered drivers appear correctly.
* Also verify Pending Verification works correctly.

2. Account Deletion Not Working
   Passenger Accounts:

* In Passengers, when opening a passenger and clicking Delete Account, an error appears.
* Find the exact cause and fix it.
* Deletion must actually remove or deactivate the account according to system rules.
* Do not leave it as a UI-only action.

Driver Accounts:

* After fixing the Drivers page, add the ability to delete driver accounts.
* Verify the delete action works end-to-end (frontend → API → database).

Admin Staff Accounts:

* In Staff Management, admin users should also be deletable.
* Verify the delete action actually works and updates the database.

3. Route Deletion Not Working

* In Routes, when deleting a route and confirming, the route is not actually removed.
* Trace the entire flow:

  * Frontend action
  * API request
  * Backend endpoint
  * Database operation
* Fix the issue so route deletion is real and persistent.
* After deletion, refresh/reload should not bring the route back.

Requirements:

* Do not use mock data.
* Do not add temporary workarounds.
* Fix the actual root causes.
* After completing the work, provide:

  1. Root cause for each issue.
  2. Files changed.
  3. Endpoints used.
  4. Confirmation that the database is actually updated.
  5. Any remaining issues discovered during the audit.
