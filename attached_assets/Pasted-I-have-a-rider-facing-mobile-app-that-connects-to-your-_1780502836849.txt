I have a rider-facing mobile app that connects to your backend. A frontend audit was performed and produced the following list of claimed issues. Please review each item against the actual backend source code and tell me:
1. Is the issue real (frontend bug)?
2. Is it a backend bug?
3. Is the audit report itself wrong?

Do NOT make any changes to the project. Just give me your analysis as a .md report.

Here are the claimed issues:

--- CRITICAL ---

C-01: App calls GET /wallet — contract says GET /wallet/balance
C-02: App calls GET /shuttle/lines — audit says this endpoint does not exist
C-03: App calls GET /trips/:id/chat and POST /trips/:id/chat — audit says these don't exist
C-04: App calls POST /users/me/push-token — audit says this doesn't exist
C-05: Login sends { credential, password } — audit says contract requires { email, password }
C-06: App calls POST /auth/forgot-password — audit says this doesn't exist
C-07: App calls POST /auth/refresh — audit says this doesn't exist
C-08: App calls POST /rides/:id/rate-driver — contract says POST /rides/:id/rate

--- HIGH ---

H-01: App listens to socket event "ride:arrived" — audit says contract emits "ride:driver_arrived"
H-02: ride:driver_assigned payload — app expects driver.vehicle (string) and eta fields, audit says they are not in the payload
H-03: App calls GET /promo (singular) — contract says GET /promos (plural)
H-04: Promo validate sends { code } only — audit says contract requires { code, amount }
H-05: Support ticket type sent as "passenger"|"driver" — audit says valid enum is "general"|"billing"|"technical"|"driver"|"ride"
H-06: App reads/sends dob field on user profile — audit says it is not in the contract
H-07: App checks rideState.status === "driver_en_route" — audit says this is not a valid contract status
H-08: App renders rideState.driver.vehicle — audit says this field is not in the socket payload

--- MEDIUM ---

M-01: booking:boarded payload — app expects { bookingId, timestamp }, audit says contract sends { bookingId, tripId, timestamp }
M-02: notification:new payload — app expects { category, time }, audit says contract sends { type, createdAt }
M-03: requestRide sends extra "notes" field not in contract
M-04: App enforces password minimum length of 8, audit says contract minimum is 6
M-05: App calls PATCH /shuttle/bookings/:id/cancel — contract says DELETE /shuttle/bookings/:id
M-06: App calls GET /shuttle/stations — audit says this endpoint is not documented
M-07: App listens to socket event "trip:chat:message" — audit says this is not in contract events

Please produce a structured .md report with a verdict for each issue.