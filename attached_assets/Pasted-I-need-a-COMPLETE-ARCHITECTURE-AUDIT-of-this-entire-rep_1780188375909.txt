I need a COMPLETE ARCHITECTURE AUDIT of this entire repository.

Do not fix anything. Do not modify code.

Generate a full deep technical report.

---

## OUTPUT REQUIREMENT (IMPORTANT)

Create a file in the project root:

ARCHITECTURE_AUDIT.md

The report must be:

* Markdown format (.md)
* Well structured with headings
* Include table of contents
* Include diagrams (text-based)
* Include file paths
* Include endpoint mappings
* Include dependency graph

After generating the file, return:

* File path
* File size
* Number of lines
* Confirmation that file was successfully created

Also optionally generate:

ARCHITECTURE_AUDIT.json

with structured machine-readable data.

---

## SECTION 1 — Repository Structure

List all applications and packages.

For each one:

* Name
* Purpose
* Runtime
* Entry file
* Port
* Dependencies

---

## SECTION 2 — Communication Map

Show how everything talks to each other:

* Admin Dashboard → API Server
* Passenger App → API Server
* API Server → Database

Include:

* Protocol (HTTP/WebSocket/etc)
* Request flow
* Response flow
* Internal service calls

---

## SECTION 3 — API INVENTORY

List ALL backend endpoints:

For each:

* Method
* Path
* Auth required
* Role required
* Source file
* Handler function

Example:

GET /api/admin/users
POST /api/auth/login
GET /api/bookings

---

## SECTION 4 — AUTHENTICATION FLOW

Explain:

* Login flow
* Token generation
* JWT verification
* Role validation
* Refresh token flow
* Security assumptions

Include exact files.

---

## SECTION 5 — ADMIN DASHBOARD DATA FLOW

For each page:

* Route
* Component file
* Hook used
* API endpoint
* Expected response

Example:

/users
→ users.tsx
→ useListAdminUsers
→ GET /api/admin/users

---

## SECTION 6 — DATABASE AUDIT

Identify:

* Direct DB access
* Shared DB layer
* Services using @workspace/db
* Any unsafe or duplicate DB connections

---

## SECTION 7 — BOOKING FLOW TRACE

Passenger booking flow:

UI → API → Service → DB → Admin dashboard display

Show full chain.

---

## SECTION 8 — USER FLOW TRACE

Signup → Login → Storage → DB → Admin visibility

---

## SECTION 9 — FAILURE POINTS

List all possible:

* 404 routes
* 401 auth failures
* 403 role failures
* 500 server errors
* 502 proxy errors

Include root cause possibilities.

---

## SECTION 10 — FINAL ARCHITECTURE DIAGRAM

Show full system:

Passenger App
Admin Dashboard
API Server
Database

Include arrows showing data flow.

---

DO NOT modify code.

ONLY analyze and generate reports.
