You are a senior backend engineer working on the VeeGo shuttle system.
I need you to fix Phase 1 issues only. Do not touch anything outside the scope below.
Fixes Required
Fix 1 — Vehicle Type Threshold (bookings.ts)
Problem: SHUTTLE_TOTAL_SEATS = 14 and SHUTTLE_MIN_REQUIRED = 7 are hardcoded constants.
This means Minibus trips (28 seats, min 14 passengers) activate too early and show wrong seat counts.
What to do:
Inside the booking transaction, fetch vehicleType and totalSeats from the trip row.
Replace the hardcoded constants with VEHICLE_CAPACITY[vehicleType] and VEHICLE_MIN_THRESHOLD[vehicleType] already defined in the schema.
Use the correct values in: activation check, available seats calculation, and the response object.
Fix 2 — Admin Reassign Must Update Trips (shuttleBookings.ts)
Problem: PATCH /admin/shuttle/bookings/:id/reassign updates driverShuttleBookingsTable but never updates tripsTable.
After reassign, trips still show the old driver.
What to do:
After updating the booking row, find all trips for that route in the same week that match the slot time.
Update those trips: set driverId to the new driver, set busId to the new driver's assigned bus.
If the new driver has no bus assigned, set trip status back to waiting_driver.
Fix 3 — Admin Cancel Must Reset Trips (shuttleBookings.ts)
Problem: PATCH /admin/shuttle/bookings/:id/cancel marks the booking as cancelled but never touches tripsTable.
Trips stay in driver_assigned state with an invalid driver reference and can never be re-assigned.
What to do:
After cancelling the booking row, find all trips linked to that driver for that route and week.
Set those trips: driverId = null, busId = null, status = "waiting_driver".
Output Required
Show only the changed code sections (not the entire file).
Clearly label which file and which lines each change applies to.
After all fixes, generate a .md report file named phase1_mobile_report.md containing:
Report Structure (phase1_mobile_report.md)
Phase 1 — Mobile Apps Impact Report
Passenger App
List every API endpoint or socket event that changed behavior in Phase 1 and how it affects the passenger app:
Endpoint or socket name
What changed
What the passenger app should now expect (new field, new value, new status, etc.)
Driver App
List every API endpoint or socket event that changed behavior in Phase 1 and how it affects the driver app:
Endpoint or socket name
What changed
What the driver app should now expect (new field, new value, new status, etc.)
Only include changes from Phase 1. Do not include future phases.