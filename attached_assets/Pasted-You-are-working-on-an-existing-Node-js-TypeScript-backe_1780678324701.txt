You are working on an existing Node.js/TypeScript backend for a multi-service ride system.

Services include:
- Shuttle (THIS IS THE ONLY SERVICE WE ARE MODIFYING)
- Car
- Motorcycle

⚠️ IMPORTANT:
All changes below MUST apply ONLY to "Shuttle service".
Do NOT modify logic for Car or Motorcycle services in any way.

────────────────────────────────────
🚨 CURRENT SHUTTLE SYSTEM (TO REMOVE)
────────────────────────────────────

In Shuttle service ONLY, remove all logic related to:
- waiting_for_driver status
- driver claiming trips
- driver assignment to trips
- driver approval before trip activation

Shuttle must no longer depend on driver actions.

────────────────────────────────────
🆕 NEW SHUTTLE SYSTEM LOGIC
────────────────────────────────────

Shuttle becomes a rider-demand based shared trip system.

────────────────────────────────────
1. TRIP CREATION (SHUTTLE ONLY)
────────────────────────────────────

When a Shuttle trip is created:
- status = OPEN
- trip is immediately visible to riders
- DO NOT assign driver
- DO NOT set waiting_for_driver

Default fields:
- capacity = 14 seats
- minRequired = 7 seats

Car and Motorcycle services remain unchanged.

────────────────────────────────────
2. BOOKING LOGIC (SHUTTLE ONLY)
────────────────────────────────────

Riders can book Shuttle trips directly.

Each booking:
- reserves 1 seat
- booking status = PENDING
- must NOT exceed 14 seats total

────────────────────────────────────
3. SHUTTLE TRIP STATUS RULES
────────────────────────────────────

For Shuttle trips ONLY:

A) OPEN
- bookedSeats < 7

B) ACTIVE
- bookedSeats >= 7
- becomes ACTIVE automatically

C) CANCELLED
- if trip start time is within 8 hours
AND bookedSeats < 7
→ cancel trip automatically

On cancellation:
- cancel all bookings
- send notifications to riders

────────────────────────────────────
4. CAPACITY RULES (SHUTTLE ONLY)
────────────────────────────────────

- totalSeats = 14
- availableSeats = 14 - bookedSeats
- prevent overbooking strictly

────────────────────────────────────
5. RIDER API RESPONSE (SHUTTLE ONLY)
────────────────────────────────────

For Shuttle endpoints ONLY, include:

- status: OPEN | ACTIVE | CANCELLED
- bookedSeats
- availableSeats
- totalSeats = 14
- minRequired = 7
- message:
  "Needs X more bookings to become active"

Car and Motorcycle responses must remain unchanged.

────────────────────────────────────
6. BACKGROUND JOB (SHUTTLE ONLY)
────────────────────────────────────

Create or update cron job logic ONLY for Shuttle:

Every 10–30 minutes:
- find Shuttle trips within 8 hours
- if bookedSeats < 7:
    → cancel Shuttle trip
    → cancel bookings
    → notify users
- else:
    → mark ACTIVE

────────────────────────────────────
7. API CHANGES (SHUTTLE ONLY)
────────────────────────────────────

Remove ONLY Shuttle-related:
- driver acceptance endpoints for Shuttle
- Shuttle driver assignment endpoints

Keep Car & Motorcycle APIs untouched.

────────────────────────────────────
8. EDGE CASES (SHUTTLE ONLY)
────────────────────────────────────

Handle:
- overbooking prevention
- duplicate booking by same user
- race conditions on booking
- consistent status updates

────────────────────────────────────
9. FINAL REPORT REQUIRED

After implementation, return a downloadable Markdown file (.md) named:

shuttle-service-report.md

This report must include:

1) Files changed (Shuttle only)
2) Logic removed (Shuttle only)
3) Logic added (Shuttle only)
4) Updated Shuttle APIs only
5) Sample Shuttle API responses
6) Status flow:
   OPEN → ACTIVE → CANCELLED
7) Confirmation that Car & Motorcycle were NOT modified

Make sure the file is properly formatted in Markdown and downloadable.

────────────────────────────────────
GOAL:
Only refactor Shuttle service into demand-based shared trip system without affecting other services.
────────────────────────────────────