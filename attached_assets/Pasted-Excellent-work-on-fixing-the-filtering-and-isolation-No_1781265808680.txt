Excellent work on fixing the filtering and isolation. Now, let's implement the bulk-import feature to allow admins to quickly seed the empty catalogs per service type.

Please implement a "Bulk Import Brands & Models" feature by adding both frontend components and backend handlers. Follow these specifications:

---

### 1. BACKEND API UPGRADE
- Create a new endpoint: `POST /api/admin/vehicle-catalog/bulk-import`.
- **Payload Structure:** The endpoint must accept:
```json
  {
    "serviceType": "scooter" | "delivery" | "shuttle" | "car",
    "catalogData": [
      {
        "brandName": "Yamaha",
        "models": ["NMAX 155", "XMAX 300", "Aerox 155"]
      },
      {
        "brandName": "Honda",
        "models": ["PCX 160", "FORZA 350"]
      }
    ]
  }
•	Database Logic: Process the import inside a safe database transaction. For each brand in the array, check if it exists for that serviceType; if not, create it. Then, map and insert all corresponding models linked to that brand ID.
2. FRONTEND UI INTEGRATION (src/components/VehicleCatalogTab.tsx)
•	Bulk Import Button: Add a "Bulk Import" button next to the "Add Brand" button.
•	Import Modal: Clicking the button must open a modal titled Bulk Import Catalog for [serviceType].
•	Input Method: Inside the modal, provide a clean, large Textarea where the admin can paste a JSON snippet (formatted like the payload structure above) or a simple comma-separated format.
•	Include a small valid JSON template/example code snippet inside the modal so the admin can copy, edit, and paste it easily.
•	Form Actions: Add a "Process Import" action button with a loading spinner state that triggers the mutation hitting the new POST bulk endpoint, followed by full query cache invalidation and a clear success toast (e.g., "Successfully imported X brands and Y models!").
REQUIREMENTS:
•	Handle JSON parsing validation errors gracefully on the client side before firing the API call.
•	Ensure strict adherence to the active serviceType context so admins cannot accidentally seed data into the wrong service.

