Please implement the following backend fixes exactly as specified:

---

## 1. Create Super Admin Account (REAL DB RECORD)

Create a persistent super admin user in the database with:

- email: info.veegoapp@gmail.com
- password: pass123
- role: superadmin (or admin if only one role exists, but must be clearly privileged)

IMPORTANT:
- This must be a REAL database record, not mock or in-memory data
- Password must be properly hashed before storage
- The account must be created via seed script or migration on server startup (only once, no duplicates)

---

## 2. Separate Authentication Systems بالكامل

Ensure strict separation between:

- Passenger authentication (users)
- Driver authentication
- Admin dashboard authentication

Rules:
- Admin login must ONLY validate admin users (role = admin/superadmin)
- Admin credentials must NOT be usable in passenger or driver login flows
- Passenger/driver accounts must NOT be able to access admin routes even if logged in successfully elsewhere
- Use separate auth guards / middleware logic if needed

IMPORTANT:
No shared session confusion between roles.

---

## 3. Fix Missing Endpoint

Fix and implement:

GET /api/admin/drivers

Requirements:
- Must return list of all drivers
- Must be protected by admin-only access middleware
- Must match existing routing prefix /api/admin/*
- Must not return 404 anymore

---

## 4. Security Requirement

- Ensure admin password is hashed (never stored in plain text)
- Ensure role checks are enforced on ALL /api/admin/* routes
- Ensure no cross-role token reuse allows privilege escalation

---

## 5. Output

After implementation, confirm:

- Admin user exists in DB
- Admin login works only for admin dashboard
- Passenger/driver login is completely isolated
- GET /api/admin/drivers returns valid data (not 403/404)

Reply with:
- What was changed
- Where it was implemented (files/locations)
- Confirmation of working endpoints