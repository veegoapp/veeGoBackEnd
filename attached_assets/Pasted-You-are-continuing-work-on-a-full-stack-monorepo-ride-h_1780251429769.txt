You are continuing work on a full-stack monorepo ride-hailing system (Uber-like platform).

IMPORTANT:
- You do NOT have any chat history.
- You must inspect the existing repository state first.
- Do NOT assume missing context.
- All previously implemented features already exist in the codebase.

=====================================================
CURRENT IMPLEMENTED SYSTEM STATE (READ ONLY FIRST)
=====================================================
The project already includes:

1. Audit Logs system (fully implemented)
2. Driver + User Location tracking system
3. Payments system (booking + ride payments + refunds)
4. Ratings system (driver ratings)
5. Partial Chat system analysis (NOT completed)

All of these are already partially or fully implemented in:
feature-reports/feature-1.md

=====================================================
YOUR TASK
=====================================================

You are NOT building from scratch.

You must:
1. Inspect current backend + frontend + DB schema
2. Identify production-level gaps
3. Fix and harden system architecture
4. Complete unfinished Chat system (Step 5)
5. Add missing production infrastructure

=====================================================
STEP 1 — PRODUCTION HARDENING (CRITICAL FIXES)
=====================================================

Implement a proper background job system to replace ALL fire-and-forget calls.

Create:
/lib/queue/jobQueue.ts

Features:
- enqueue(job)
- async worker processor
- retry failed jobs (max 3 attempts)
- exponential backoff (1s → 5s → 15s)
- dead letter queue storage

MUST convert ALL of the following into queued jobs:
- audit logs writes
- driver location inserts
- payments inserts
- ratings inserts

NO direct void async writes allowed anymore.

=====================================================
STEP 2 — FIX RATINGS DATA CONSISTENCY
=====================================================

Problem:
ratingsTable and rideEventsTable both store rating-related data.

Fix:
- ratingsTable must be SINGLE source of truth
- rideEventsTable remains ONLY for event tracking

Changes:
- Update GET /driver/me/ratings → read ONLY from ratingsTable
- Add DB constraint: UNIQUE (rater_id, ride_id)
- Prevent duplicate ratings at DB level

=====================================================
STEP 3 — OBSERVABILITY (TRACE SYSTEM)

Create:
/lib/observability/trace.ts

Add:
- generateTraceId() per request
- attach traceId to req
- include traceId in logs, audit logs, payments, ratings, locations

Middleware:
- assign traceId at request start

Update logger (pino):
- include traceId, userId, route, method

=====================================================
STEP 4 — TEST AUTOMATION

Add backend tests:

/tests/setup.ts
/tests/auditLogs.test.ts
/tests/payments.test.ts
/tests/ratings.test.ts

Use existing project testing stack (detect jest/vitest/etc).

Must include:
- vehicle create → audit log created
- booking complete → payment created
- ride rated → rating stored

Add npm script:
"test": "run backend tests"

=====================================================
STEP 5 — COMPLETE CHAT SYSTEM (STEP 5)

Finish chat implementation:

Backend:
- GET /api/chat/trips/:tripId/messages
- POST /api/chat/trips/:tripId/message
- PATCH mark messages as read

Socket events:
- driver:message
- passenger:message
- admin:chat-stream

Frontend:
- Admin page: /chat-inbox
- Group messages by tripId
- Show unread counters
- Real-time updates via sockets

Ensure:
- persistence in DB
- real-time sync via sockets

=====================================================
STEP 6 — UPDATE FEATURE REPORT

Append ONLY to:
feature-reports/feature-1.md

DO NOT overwrite existing content.

Add section:

## Step 5: Production Hardening + Chat Completion

Include:
- job queue system
- retry mechanism
- observability trace system
- ratings deduplication fix
- test automation
- chat system completion

Keep same structure style as previous steps.

=====================================================
RULES
=====================================================
- Do NOT delete existing features
- Do NOT refactor unrelated code
- Only extend + fix production gaps
- Ensure build compiles successfully
- Ensure API backward compatibility
- Ensure system stability after restart