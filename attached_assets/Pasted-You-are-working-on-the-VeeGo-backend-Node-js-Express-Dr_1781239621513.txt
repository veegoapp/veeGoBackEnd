You are working on the VeeGo backend (Node.js + Express + Drizzle ORM + PostgreSQL).

ONE task only: connect the Shuttle (شاتيل) service to the existing 
milestone bonus system that was built in Phase 4.

Currently the bonus progress system only runs after car/scooter/delivery 
ride completion. Shuttle trips are completely disconnected.

---

## CONTEXT

The bonus system works like this:
- `driver_bonus_targets` table: admin defines targets 
  (e.g. "complete 50 shuttle trips = 200 EGP bonus")
- `driver_bonus_progress` table: tracks each driver's progress per target
- `artifacts/api-server/src/lib/bonus-targets.ts`: 
  shared helper `updateBonusProgressAfterRide(driverId, serviceType, finalPrice)`
  - This function is currently called only from `rides.ts` after car/scooter/delivery completion
- `service_type` field on `driver_bonus_targets` supports `"shuttle"` already 
  but nothing ever triggers it

---

## WHAT TO DO

### Step 1 — Find shuttle trip completion in the backend

Look in `artifacts/api-server/src/routes/driver.ts` (or wherever shuttle trip 
completion is handled) for the endpoint where a driver marks a shuttle trip 
as completed.

This is likely:
- `PATCH /driver/trips/:id/complete` or similar
- The point where `trips.status` is set to `"completed"`

### Step 2 — Call updateBonusProgressAfterRide after shuttle completion

After the trip status is set to `"completed"` and earnings are recorded:

1. Get the driver's `driverId` and `userId`
2. Get the trip's final price (the amount the driver earned, 
   after commission deduction)
3. Call:
```typescript
   updateBonusProgressAfterRide(driverId, "shuttle", driverEarnings)
```
   Wrap in try/catch — non-fatal (bonus failure must NEVER break trip completion)

### Step 3 — Verify the helper handles "shuttle" correctly

In `artifacts/api-server/src/lib/bonus-targets.ts`:

Check that `updateBonusProgressAfterRide` correctly matches shuttle trips to targets:
- A target with `service_type = "shuttle"` → matches only shuttle completions
- A target with `service_type = "all"` → matches shuttle AND rides
- A target with `service_type = "car"` → does NOT match shuttle

If the matching logic doesn't handle "shuttle" correctly, fix it now.

### Step 4 — Also connect criminal record check to shuttle completion

In Phase 2 we built `checkCriminalRecordThreshold(driverId, driverUserId)`.
This was connected to ride completion but NOT to shuttle trip completion.

After shuttle trip completion (same place as Step 2), also call:
```typescript
checkCriminalRecordThreshold(driverId, driverUserId)
```
Wrap in try/catch — non-fatal.

### Step 5 — Test

Confirm:
- A shuttle trip completion triggers `updateBonusProgressAfterRide` 
  with serviceType "shuttle"
- A bonus target with `service_type = "shuttle"` gets its progress updated
- A bonus target with `service_type = "all"` also gets updated
- A bonus target with `service_type = "car"` does NOT get updated
- Criminal record check also fires after shuttle completion

---

## REPORT

Generate a file called `phase4b_shuttle_bonus_report.md`:

# VeeGo Backend — Phase 4b: Shuttle Bonus Connection Report

## What was changed
- File name + exact line numbers for every change

## Shuttle trip completion hook
- Which endpoint handles shuttle completion
- What was added after completion

## Bonus matching logic
- Was it already correct for "shuttle"? or did it need fixing?
- How service_type matching works now

## Criminal record check
- Was it already connected to shuttle? (yes/no)
- What was added

## Test results
- Each test case with result