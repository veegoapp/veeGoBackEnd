Please execute a comprehensive frontend upgrade on the Admin Dashboard source code (under `artifacts/admin-dashboard/src/`) based on the verification report "veego-frontend-audit.md". You must build all completely missing views, implement dynamic inputs for existing APIs, create new explicit pages, and wire all new routes into `App.tsx`.

Follow this structured, file-by-file implementation plan step-by-step:

---

### 1. UPDATE EXISTING PAGES & COMPONENTS

#### A. Driver Detail Workspace (`src/pages/driver-detail.tsx`)
- **Criminal Record Enforcement Counter:** inside the Account Status card (Overview tab), fetch the compliance state from `POST /admin/drivers/:id/check-criminal-record`. Render a progress row (e.g., "Criminal Record Compliance: X / 30 trips"). If trips >= 30 and the document is not approved, replace the generic "Suspended" badge with an explicit red banner: "Suspended: Criminal Record Threshold Exceeded". Add a manual button "Trigger Criminal Record Check".
- **Per-Driver Commission Override:** Add a dedicated input row under the status panel. Allow admins to input a customized commission percentage (0-100%). On save, map it to a decimal and call `PATCH /api/admin/drivers/:id/commission`. Add a "Clear Override" button that passes `commissionRate: null` to fall back to the system global rate.
- **Bonus Progress Tab:** Create a new tab inside the driver's profile named "Bonus Milestones". Fetch data using `GET /admin/drivers/:id/bonus-progress` and display dynamic progress bars for each active target (e.g., "Completed 15 out of 40 trips").

#### B. Centralized Settings Page (`src/pages/settings.tsx`)
- **System Global Parameters Tab:** Add a new "System Engine" tab to the existing layout. Render editable numeric fields wired to fetch/update individual keys via `GET /admin/settings/:key` and `PATCH /admin/settings`. Expose these seeded backend variables with clear labels:
  - `dispatch_radius_km` (Initial Search Radius)
  - `dispatch_max_radius_km` (Max Search Radius Ceiling)
  - `dispatch_offer_timeout_seconds` (Driver Offer Timeout)
  - `no_show_fee_egp` (Shuttle No-Show Fee)
  - `cancellation_grace_hours` (Free Cancellation Window)
  - `criminal_record_trip_threshold` (Suspension Limit - Default 30)
- **Wallet Financial Limits Card:** Inside this new tab, add an isolated section for wallet rules. Map input values to `PATCH /admin/settings/wallet-limits` for adjusting `wallet_max_topup` and `wallet_daily_topup_limit`.

#### C. Shuttle Fleet Management (`src/pages/buses.tsx`)
- **Shuttle Vehicle Types Sub-Tab:** Build an independent view alongside the vehicle catalog. Implement a full CRUD grid connecting to `GET/POST /admin/shuttle/vehicle-types` and `GET/PATCH/DELETE /admin/shuttle/vehicle-types/:id`. Fields must include: Name (e.g., Hiace, Minibus), Capacity (Total Seats), Min Threshold (Seats required to launch), and an IsActive toggle.

#### D. Vehicle Catalog Tab (`src/components/VehicleCatalogTab.tsx`)
- **Approved Colors Catalog Panel:** Add a third management grid directly underneath the existing Brands and Models grids. Wire it to manage the `vehicle_colors` database table via standard catalog patterns. Implement a color picker input (`type="color"`) to capture hex codes alongside English/Arabic name strings.

---

### 2. CREATE NEW STANDALONE PAGES

#### A. Fraud & Security Center (`src/pages/fraud-alerts.tsx`)
- Create a brand new security review module.
- Render a live table fetching paginated records from `GET /admin/duplicate-alerts?resolved=false`. 
- Columns: Alert ID, New Driver Name, Existing Driver Name, Match Type (National ID), Timestamp, Action.
- Add a "Resolve Alert" action button per row that triggers a popup modal with a text area for audit notes, firing `PATCH /admin/duplicate-alerts/:id/resolve` on submission.
- Integrate real-time socket listeners (`admin:duplicate_driver_alert`) to flash a warning toast and update the active counter badge.

#### B. Commission Exemptions Manager (`src/pages/commission-exemptions.tsx`)
- Create a scheduling interface under the financial workspace.
- Implement a CRUD data table linked to `/api/admin/commission-exemptions`.
- Provide a "Create Exemption Period" modal featuring a searchable driver selector dropdown, date-time pickers for `starts_at` and `ends_at`, an active status toggle, and an optional text area for logging operational reasons.

#### C. Milestone Bonus Configuration Portal (`src/pages/bonus-targets.tsx`)
- Create an incentive manager dashboard with a master-detail structure.
- **Master Setup:** A CRUD grid for `/api/admin/bonus-targets` to create incentives. Inputs: Name, Service Type (Car, Shuttle, Bike, etc.), Target Type (`ride_count` or `earnings_amount`), Target Target Value, Bonus Payout Amount (EGP), and Window Timeline (Start/End Date-Times).
- **Detail Drilldown:** Clicking an active target row must display the progress view using data from `GET /admin/bonus-targets/:id/progress`, listing all enrolled drivers, their live percentage completion bars, and completion timestamps.

---

### 3. ROUTING & UI INTEGRATION

#### A. Global Router Upgrades (`src/App.tsx`)
- Import and register the three new pages with explicit path definitions:
  - `<Route path="/security/fraud-alerts" component={FraudAlerts} />`
  - `<Route path="/finance/commission-exemptions" component={CommissionExemptions} />`
  - `<Route path="/finance/bonus-targets" component={BonusTargets} />`
- Update the main Sidebar Navigation shell to expose these new views under their respective "Security" and "Finance" categories, maintaining consistent icon designs and layout constraints.

#### B. Contextual UI Alerts & Confirmations
- **Driver Verification View (`src/pages/driver-verification.tsx`):** Calculate the approved document count for the selected driver. When exactly 7 out of 8 required documents are approved, display a noticeable yellow warning callout box: "Approving this final document will trigger immediate automatic account activation for this driver."
- **Trips Management Action Handler (`src/pages/trips.tsx`):** Enhance the success outcome handler for `PATCH /trips/:id/cancel` and `DELETE /trips/:id`. Instead of a generic success toast, intercept or fetch the refund data payload to show a summary toast modal stating: "Trip Cancelled Successfully: X linked bookings have been fully refunded to customer wallets."

---

### REQUIREMENTS:
- Adhere strictly to the existing codebase styling, design tokens, loading state wrappers, and error boundary handling.
- Verify that all query cache invalidations trigger properly upon successful mutations to prevent stale data displays.