Excellent work on Phase 1! Everything compiled cleanly. Now, let's proceed immediately to Phase 2.

Please execute Phase 2 of the dashboard overhaul to implement the dual-tab "Vehicle Management Center" across all 4 services, fully connected to the existing backend APIs.

Follow these exact steps:
1. Restructure Pages Using a Dual-Tab Layout:
   - For Car (`/vehicles/car`), Scooter (`/vehicles/motorcycle`), Delivery (`/vehicles/delivery`), and Shuttle (`/vehicles/shuttle`), restructure the page layout to include two main navigation Tabs:
     - Tab 1: "Registered Fleet" (Holds the existing table displaying plates, driver assignments, and approval statuses).
     - Tab 2: "Allowed Catalog" (New view to visually manage eligible brands and models).

2. Implement Tab 1 ("Registered Fleet" UI):
   - Ensure the existing searchable table is visible ONLY when Tab 2 is NOT selected.
   - For the Shuttle service page (`/vehicles/shuttle`), explicitly add and display a "Seat Capacity" column in this table.

3. Implement Tab 2 ("Allowed Catalog" UI) & Connect APIs:
   - Split Tab 2 UI into two clean sections/tables: "Approved Brands" and "Approved Models".
   - Connect "Approved Brands" to the backend API: Fetch data using `GET /api/admin/vehicle-catalog/brands` (filtered by current service type). Add an "Add New Brand" button triggering a `POST` request. Add Edit (PATCH) and Delete (DELETE) actions.
   - Connect "Approved Models" to the backend API: Fetch data using `GET /api/admin/vehicle-catalog/models` (filtered by selected brand and service type). Add an "Add New Model" button triggering a `POST` request. Add Edit (PATCH) and Delete (DELETE) actions.

4. Integrate Shuttle-Specific Seat Capacity Controls:
   - In the Shuttle Vehicles page, ensure both Tab 1 forms and Tab 2 "Add/Edit Model" forms include a dedicated numeric field for "Seat Capacity" (e.g., 14 seats for a microbus, 28 seats for a minibus) and map them directly to the existing backend database schema.

---
### REQUIRED OUTPUT FOR VERIFICATION:
Provide a comprehensive checklist showing all updated/created files, confirming that the dual-tab layout is active for all 4 services, and that Seat Capacity inputs/columns are working seamlessly with the server.