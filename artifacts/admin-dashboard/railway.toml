[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @workspace/admin-dashboard run build"

[deploy]
startCommand = "pnpm --filter @workspace/admin-dashboard run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
